#include<stdio.h>
struct rect {
	int width;
	int height;
	// 구조체를 정의하라.
};
int area(struct rect r){
	return r.width * r.height;	
}

// 함수를 정의하라.

int main(){
	struct rect r;
	scanf("%d %d", &r.width, &r.height);
	int ar = area(r);
	printf("면적은 %d\n", ar);
	return 0;
}
